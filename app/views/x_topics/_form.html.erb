<%= form_with(model: x_topic) do |form| %>
  <% if x_topic.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(x_topic.errors.count, "error") %> prohibited this x_topic from being saved:</h2>

      <ul>
        <% x_topic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :query, style: "display: block" %>
    <%= form.text_field :query %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
